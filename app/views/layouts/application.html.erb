<!DOCTYPE html>
<html>
  <head>
    <title>Friendzone</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Kani", media: "all" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body data-current-user-id="<%= current_user ? current_user.id : "" %>">
    <!-- Afficher la navbar sauf pour les actions spécifiques -->
    <% unless controller_name == "pages" && action_name == "home" || controller_name == "sessions" || controller_name == "registrations" %>
      <%= render "shared/navbar" %>
    <% end %>

    <%= yield %>

    <!-- Afficher le footer sauf pour les contrôleurs et actions spécifiés -->
    <%= render 'shared/footer' unless controller_name == "pages" && action_name == "home" || controller_name == "sessions" || controller_name == "registrations" %>

  </body>
</html>
